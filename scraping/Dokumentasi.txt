Paket/Library yang harus di install:

1. requests
2. bs4
3. selenium

paket re dan time biasanya sudah terinstall otomatis ketika instalasi Python3


install browser chromium dan drivernya:

	pip install chromedriver-autoinstaller

	sudo apt-get update

	sudo apt install chromium-browser





